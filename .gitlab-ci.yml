stages:
  - build
  - deploy

generate_news:
  stage: build
  image: python:3.11-slim
  script:
    - pip install requests
    - python generate_tech_news.py
  only:
    - schedules

pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp -r ./*.html ./*.txt ./*.xml public/ 2>/dev/null || true
    - cp -r fix locations repair brands tools blog unlock news public/ 2>/dev/null || true
  artifacts:
    paths:
      - public
  only:
    - main